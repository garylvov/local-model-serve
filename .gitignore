# weights, builds, envs, runtime state
/models/
/vendor/
/run/
/.venv/
/.cache/
__pycache__/
*.pyc
# registry: live node files and the real static file are local state
/registry/*.json
/registry/static.yaml
# never commit secrets
*.key
*.env
!*.env.example
/mcp/.venv/

# site-specific settings (see config.example.env)
/config.env
# working notes, not part of the project
/tasks/
/mcp/.npm/
/.playwright-mcp/
!config.example.env
